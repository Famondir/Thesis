apiVersion: batch/v1
kind: Job
metadata:
    name: llama4-scout-17b-job
    # namespace: s92818
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: llama4-scout-17b-job
    spec:
      containers:
      - name: llama4-scout-17b-job
        image: registry.datexis.com/s92818/remote-work
        command: ["/bin/sh", "-c"]
        args: [
          "python3 -u /pvc/thesis_benchmarks/page_detection_benchmark_llm.py --model_name meta-llama/Llama-4-Scout-17B-16E"
        ]
        resources:
          requests:
              cpu: "4"
              memory: "32Gi"
              nvidia.com/gpu: "4"
          limits:
            nvidia.com/gpu: "4"
            memory: 256Gi
            cpu: "32"
        env:
          - name: LANG
            value: 'C.UTF-8'
          - name: HF_HOME
            value: "/pvc/hub_cache"
          - name: OMP_NUM_THREADS
            value: "1"
          - name: HUGGING_FACE_HUB_TOKEN
            valueFrom:
              secretKeyRef:
                name: hf-token-secret
                key: token
        volumeMounts:
          - name: vllm-pvc
            mountPath: /pvc
      nodeSelector:
        # kubernetes.io/hostname: cl-worker29
        gpu: b200
      imagePullSecrets:
        - name: private-registry-auth
      volumes:
        - name: vllm-pvc
          persistentVolumeClaim:
            claimName: vllm-pvc
      restartPolicy: Never